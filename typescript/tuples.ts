// type personinfo = [string, number, boolean]


// const driver: (person: personinfo) => void = (person: personinfo): void => {
//     const { name, age, hasdrive } = person;
//     console.log(`${name} is ${age} years old it${hasdrive} ?"yes":"No"`)
// }
// const person1: personinfo = ["myank", 23, true]
// driver(person1)
// type personinfo = [string, number, boolean];

// const driver: (person: personinfo) => void = (person: personinfo): void => {
//     const [name, age, hasdrive] = person;
//     console.log(`${name} is ${age} years old. Does ${name} have a driver's license? ${hasdrive ? "Yes" : "No"}`);
// }

// const person1: personinfo = ["Mayank", 23, true];
// driver(person1);

